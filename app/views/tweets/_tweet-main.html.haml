.tweet-main
  .tweet-box
    .tweet-user
      .profile-image
        = image_tag current_user.image, size: '50x50' if current_user.image.present?
      .tweet-form
        = form_for @tweet do |f|
          .tweet-form-top
            = f.text_field :content, placeholder:'今何してますか？', class:'tweetForm'
          = f.label :image, class:'tweet-image' do
            = f.file_field :image, class:'image'
            %i.fa.fa-image
          = f.submit "Send", class:'submit'
  .tweet-container
    .new-tweet-bar
      .tweet-nofitication
        新着ツイートなし
    .tweet-list
      = render 'tweet'
